// The whole UI lives in the persistent layout so that all three systems stay
// mounted for the transitions. The routes only carry metadata and static params.
export default function HomePage() {
  return null;
}
